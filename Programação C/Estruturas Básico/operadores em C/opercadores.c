#include <stdio.h>


int main()
{
    int x;
    int y;

    x = 50 ; // ( 1 de igual significa recebe )
    y = 20 ;

    printf("%d", x+y );

                            // aritméticos: (+ adição), (- subtração), (* multiplicação), (/ divisão), (% resto da divisão "mod")

                            // Comparativos: ( < menor que )( > maior que),( <= menor ou igual) (> maior ou igual ), ( == igual ),(!= diferente)

                            // Lógicos: (&& e ), (|| ou ), (! não )

    return 0;
}
